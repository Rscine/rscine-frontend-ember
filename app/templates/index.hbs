<h1>Users:</h1>

{{#each model as |user|}}
    <h2>{{user.username}}</h2>
    <ul>
        <li>{{user.email}}</li>
    </ul>
    {{#each contactInformations as |infos|}}
        {{infos}}
    {{/each}}
{{/each}}
